<%= form_for(@house_work) do |f| %>
  <% if @house_work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@house_work.errors.count, "error") %> prohibited this house_work from being saved:</h2>

      <ul>
      <% @house_work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipe_id %><br>
    <%= f.text_field :recipe_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
